<%= form_with(model: [version.feature, version]) do |form| %>
  <% if version.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(version.errors.count, "error") %> prohibited this version from being saved:</h2>

      <ul>
        <% version.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :feature_id, style: "display: block" %>
    <%= form.text_field :feature_id %>
  </div>

  <div>
    <%= form.label :current, style: "display: block" %>
    <%= form.check_box :current %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
